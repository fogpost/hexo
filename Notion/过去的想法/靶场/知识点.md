## nmap

nmap (-sS -sT -sU) -P -sV -O
nmap -sS -sV -O -p- 目标IP SYN、服务识别、操作系统识别、扫描所有端口
-sn 主机发现
--script=vuln IP 脚本

## Feroxbuster
Feroxbuster -u IP -w .txt -x php -o

## Redis
三个关键可修改项
CONFIG SET dir
CONFIG SET dbfilename
SAVE