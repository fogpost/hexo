---

---
## SUID

```python
find / -user root -perm -4000 -print 2>/dev/null
result:
(www-data:/var/www/html) $ find / -user root -perm -4000 -print 2>/dev/nul
l
/var/www/html/wc
/bin/umount
/bin/mount
/bin/su
/usr/bin/newgrp
/usr/bin/passwd
/usr/bin/chfn
/usr/bin/gpasswd
/usr/bin/chsh
```

利用网址[https://gtfobins.github.io/](https://gtfobins.github.io/)
