数据来源：抓包、导包
文件管理，统一到data目录，支持目录扫描，顺序解析，游标推荐
数据加载：fastapi调用scapy读取pcap，缓存为内存包列表，
协议解析，按照层拆解字符，输出偏移长度，可读值与hex高亮联动
特征提取，统计协议分布、源/目的ip聚合
规则检测：基于规则库生成告警输出可解释的证据
前端展示：包列表、包详情、hex视图与一键定位器
桌面增强（wails）：内置后端启动，网卡选择与ssh交互式shell

1. API 总入口与路由：  
    [[main.py]]:51
2. pcap 读取与内存缓存：  
    [[pcap_loader.py]]:11
3. data 目录文件编目与顺序解析：  
    [[pcap_catalog.py]]:16
4. 包详情协议解析与可读化：  
    [[packet_detail_parser.py]]:172
5. 规则检测与告警证据输出：  
    [[detection_engine.py]]:47
6. Go 实时抓包与网卡筛选：  
    [[main.go]]:122
7. Wails 桌面编排（网卡列表、终端、SSH 长连接 shell）：  
    [[app.go]]:591
8. 桌面前端 UI（同款交互）：  
    [[App.vue]]:256
9. Web 前端 UI（已同步）：  
    [[App.vue]]:278
10. 一键启动脚本：  
    [[start_all.ps]]1:9

完善点：
1. 规则引擎升级为“规则 + 阈值自适应 + 时序窗口”。
2. 告警证据增加“字段级因果链”与“规则命中路径图”。
3. 加入流级会话重组（5 元组）和应用层协议识别。
4. 增加基于模型的异常检测，与规则结果做融合评分。
5. 引入数据集与基准评测：准确率、召回率、误报率、时延、吞吐。
6. 终端模块加入审计日志与权限控制（多用户/角色）。
7. 抓包存储做分片与归档策略，支持高流量场景。
8. 增加跨平台网卡状态采集统一层（Windows/Linux/macOS 一致化）。